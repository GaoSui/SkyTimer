<UserControl x:Class="SkyTimer.View.StatisticView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SkyTimer.View"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="600"
             DataContext="{Binding Statistic,
                                   Source={StaticResource Locator}}"
             mc:Ignorable="d">
    <UniformGrid Columns="3" Rows="3">
        <Label Content="{Binding Best}" />
        <Label Content="{Binding Best5}" />
        <Label Content="{Binding Current5}" />
        <Label Content="{Binding Worst}" />
        <Label Content="{Binding Best12}" />
        <Label Content="{Binding Current12}" />
        <Label Content="{Binding All}" />
        <Label Content="{Binding Best100}" />
        <Label Content="{Binding Current100}" />
    </UniformGrid>
</UserControl>
